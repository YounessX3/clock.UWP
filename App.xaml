<Application
x:Class="clock.UWP.App"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="using:clock.UWP"
RequestedTheme="Light">


    <Application.Resources>
        <!-- Acrylic brush (Fluent Design) -->
        <AcrylicBrush x:Key="AppAcrylicBrush"
BackgroundSource="HostBackdrop"
TintColor="#FF0078D7"
TintOpacity="0.60"
FallbackColor="#FF0078D7" />


        <!-- Cubic (square) button style used throughout -->
        <Style x:Key="SquareButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Simple text styles -->
        <Style TargetType="TextBlock" x:Key="LargeDisplay">
            <Setter Property="FontSize" Value="48" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>


    </Application.Resources>
</Application>